import { PageStub } from "@/components/PageStub";

export function HowItWorks() {
  return (
    <PageStub title="How it works">
      The pipeline end to end: parse-then-chunk ingest, dense retrieval
      (dense-ctx-v1), and the grammar-ReAct agent.
    </PageStub>
  );
}
