import { PageStub } from "@/components/PageStub";

export function Security() {
  return (
    <PageStub title="Security" mockup="security-01-table.html">
      The deterministic guard stack and red-team lab results (Phase 6.3).
    </PageStub>
  );
}
