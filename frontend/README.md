# Frontend

TypeScript app — chat with inline citations, evals tab, cost display, "how it works" page.

**Not started.** Built in Phase 7 (see [project-plan.md](../project-plan.md));
Next.js vs Vite SPA is decision gate **D4**, taken when UI work begins.
Until then the backend's auto-generated docs (`uv run ahx serve` → /docs) are the UI.
