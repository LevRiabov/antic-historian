services:
  db:
    image: pgvector/pgvector:pg18
    container_name: ahx-db
    # Survive Docker Desktop / host restarts (outage observed 2026-06-12:
    # daemon restart left this container down, only one without a policy).
    restart: unless-stopped
    environment:
      POSTGRES_USER: ahx
      POSTGRES_PASSWORD: ahx
      POSTGRES_DB: ahx
    ports:
      # Host 5433: rag-historian's ParadeDB already owns 5432 on this machine.
      - "5433:5432"
    volumes:
      - ahx-pgdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ahx -d ahx"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  ahx-pgdata:
